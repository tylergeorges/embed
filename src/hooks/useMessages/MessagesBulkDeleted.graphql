subscription MessagesBulkDeleted($channels: [String!], $guild: String!, $threadId: String) {
  messageDeleteBulk: messageDeleteBulkV2(channels: $channels, guild: $guild, threadId: $threadId) {
    ids
  }
}
