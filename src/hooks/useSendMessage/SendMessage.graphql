#import "../useMessages/Fragments/Message.graphql"

mutation SendMessage($channel: String!, $content: String!, $thread: String) {
  sendMessage(channel: $channel, content: $content, threadId: $thread) {
    ...Message
  }
}
