query Channels($guild: String!) {
  guild(id: $guild) {
    channels {
      __typename

      name
      id
      position
      nsfw
      canSend
      
      category {
        name
        position
      }
    }
  }
}
