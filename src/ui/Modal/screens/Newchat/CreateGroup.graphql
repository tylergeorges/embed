#import "../../../Sidebar/Chats/Chat.graphql"

mutation CreateGroup($guild: String!, $memberIds: [String!]!, $content: String!) {
  createGroup(guild: $guild, memberIds: $memberIds, content: $content) {
    ...Chat
  }
}
