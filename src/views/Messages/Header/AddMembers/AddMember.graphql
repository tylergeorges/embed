mutation AddMember($guild: String!, $chat: String!, $members: [String!]!) {
  addMember(guild: $guild, id: $chat, memberIds: $members) {
    id
    name
    discrim
    avatarUrl
    color
    bot
    flags
  }
}
