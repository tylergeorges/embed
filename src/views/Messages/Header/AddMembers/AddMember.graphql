mutation AddMember($guild: String!, $chat: String!, $member: String!) {
  addMember(guild: $guild, id: $chat, memberIds: [$member]) {
    id
    name
    discrim
    avatarUrl
    color
    bot
    flags
  }
}
