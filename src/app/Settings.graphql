query Settings($guild: String!) {
  settings(id: $guild) {
    guestMode
    invite
    singleChannel
    hideSidebar
    readonly
    directEnabled
    theme {
      css
      colors {
        primary
        accent
        background
      }
    }
  }
}
