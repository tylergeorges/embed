query Settings($guild: String!) {
  guild(id: $guild) {
    id
    settings {
      guestMode
      invite
      singleChannel
      readonly
      theme {
        css
        colors {
          primary
          accent
          background
        }
      }
    }
  }
}
