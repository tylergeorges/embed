query Settings($guild: String!) {
  settings(id: $guild) {
    guestMode
    invite
    singleChannel
    hideSidebar
    readonly
    filesEnabled
    isCustomAuthEnabled
    isCaptchaEnabled
    theme {
      css
      colors {
        primary
        accent
        background
      }
    }
  }
}
