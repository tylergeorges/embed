query Settings($guild: String!) {
  settings(id: $guild) {
    guestMode
    invite
    singleChannel
    hideSidebar
    readonly
    directEnabled
    filesEnabled
    isCustomAuthEnabled
    theme {
      css
      colors {
        primary
        accent
        background
      }
    }
  }
}
